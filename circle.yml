machine:
  pre:
    - curl -sSL https://s3.amazonaws.com/circle-downloads/install-circleci-docker.sh | bash -s -- 1.10.0
  java:
    version: oraclejdk8
  services:
    - docker
  environment:
    PROFILE: testGemFlux
    CHECKDIR: checksums

test:
  override:
    - ./test.sh